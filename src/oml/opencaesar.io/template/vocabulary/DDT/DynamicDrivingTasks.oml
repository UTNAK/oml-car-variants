@dc:title "Example Vocabulary"
@dc:creator "Example Company"
@dc:rights "Copyright 2019, by Example Company"
vocabulary <http://opencaesar.io/template/vocabulary/DDT/DynamicDrivingTasks#> as DynamicDrivingTasks {

	extends <http://purl.org/dc/elements/1.1/> as dc
    extends <http://www.w3.org/2000/01/rdf-schema#> as rdfs
    extends <http://www.w3.org/2001/XMLSchema#> as xsd


	extends <http://opencaesar.io/template/vocabulary/ADSdomain#> as adsdomain
    extends <http://imce.jpl.nasa.gov/foundation/mission#> as mission
    extends <http://www.w3.org/2003/11/swrlb#> as swrlb


    concept DynamicDrivingTask < mission:Objective[    
    ]

	@rdfs:label "Parking"
	@dc:description "ADS comes to a complete stop within a vacant parking spot; may be further qualified by parallel or perpendicular orientations, lot type (closed/open), initiation conditions, etc."
    concept Parking < DynamicDrivingTask
    // NHTSA p.128

    @rdfs:label "Maintain Speed"
    @dc:description "ADS maintains a safe speed set through longitudinal control with acceptable following distances"
    concept Maintain-Speed < DynamicDrivingTask[
        restricts TargetVelocity to exactly 1
    ]
    // NHTSA p.121 -> no leading vehicle or far away leading vehicle -> verify Maintain Speed + Lane Centering

    @rdfs:label "Vehicle Following"
    @dc:description "ADS identifies and follows a target vehicle at acceptable following distance while staying within a lane through longitudinal and lateral control."
    concept Vehicle-Following  < DynamicDrivingTask[
        restricts AcceptableFollowingDistance to exactly 1
        // needs to add acceptable following distance <scalar property>
    ]
    // NHTSA p.121
    scalar property AcceptableFollowingDistance[
        domain Vehicle-Following
        range xsd:string
        functional  
    ]

    @rdfs:label "Lane Centering"
    @dc:description "ADS stays within a lane through lateral control."
    concept Lane-Centering  < DynamicDrivingTask[
        // needs to have line marking << ADD Rule: RoadFurniture:LineMarkers
    ]
    // NHTSA p.121 -> no leading vehicle or far away leading vehicle -> verify Maintain Speed + Lane Centering

    @rdfs:label "Lane Switching/Overtaking"
    @dc:description "ADS crosses lanes or overtakes an upcoming vehicle based on a projected path or hazard."
    concept Lane-Switching-Overtaking  < DynamicDrivingTask[
        // Rule: Overtaking must have more than POV
        // Rule: Must have more than one lane
        // Add domain ontology : Lane Switching Context : Right lane switching or Left lane switching 
    ]
    //　NHTSA p.121 -> changing yellow solid line to white dashed line

    @rdfs:label "Enhancing Conspicuity"
    @dc:description "ADS controls vehicle blinkers, headlights, horn, or other methods used to communicate with other drivers."
    concept Enhancing-Conspicuity  < DynamicDrivingTask
    // pending for detail defiitions

    @rdfs:label "Obstacle Avoidance"
    @dc:description "ADS identifies and responds to on-road hazards,such as pedestrians, debris, animals, etc."
    concept Obstacle-Avoidance  < DynamicDrivingTask
    // add rule : must have NonRoadUser in ODD (JASPAR: Traffic Participants）
    // restriction: NonRoadUser will affect SV
    // extended domain ontology?
    // refer to papers related to obstacle avoidance (search for papers)

    @rdfs:label "Low-Speed Merge"
    @dc:description "ADS merges into a lane below about 45 mph, for example from
    an exit ramp, by identifying a vacant lane position and matching speed."
    concept Low-Speed-Merge  < DynamicDrivingTask[
        restricts maxVelocity to exactly 1
        // rule: odd must have two or more lanes
    ]
    concept Baseline < Low-Speed-Merge[
        // restricts surrounds to max 0
    ]
    concept Simple-Positive < Low-Speed-Merge[
        // restricts surrounds to exactly 1
    ]
    concept Complex-Positive < Low-Speed-Merge[
        // restricts surrounds to exactly 2
    ]
    concept Simple-Negative < Low-Speed-Merge[
        // restricts surrounds to min 3
    ]
    // NHTSA p.115 + ramp

    @rdfs:label "High-speed Marge"
    @dc:description "ADS merges into alane above about 45 mph, for example from an exit ramp, by identifying a vacant lane position and matching speed."
    concept High-speed-Marge  < DynamicDrivingTask[
        restricts minVelocity to exactly 1
        // rule: odd must have two or more lanes
    ]
    // NHTSA p.115 (change speed) + ramp

	@rdfs:label "Navigate On/Off-Ramps"
	@dc:description "ADS drives on on/off-ramps, which are typically one-way, steeply curved, and banked road segments."
	concept Navigate-OnOff-Ramps < DynamicDrivingTask
    // rule: scene must have on or off ramps
    // focuse on one-way, steeply curved, and banked road segments. Select appropriate position in Carla

    @rdfs:label "Follow Driving Laws"
	@dc:description "ADS obeys motor vehicle codes and local ordinances; for example, following distances, speed limits, etc. This may include driving norms that vary by region as well."
	concept Follow-Driving-Laws < DynamicDrivingTask
    // how can we use ontology to define regional rules?
    // property: Applied Driving Laws: Specification/ region, nationality
    // pending

    @rdfs:label "Right-of-Way Decisions"
	@dc:description "ADS obeys directional restrictions; for example, one-way roads and actively managed lanes."
	concept Right-of-Way-Decisions < DynamicDrivingTask
    // property: Applied Driving Laws, 
    // Rule: must have information of Zones or Signs, 
    // pending


    @rdfs:label "Navigate-Roundabouts"
    @dc:description " ADS determines right-of-way, enters, navigates, and exits a 
     roundabout, and communicates with other road users as necessary. "
    concept Navigate-Roundabouts  < DynamicDrivingTask[
        //restricts maxVelocity to exactly 1
    ]
    // rule; ood must have Roundabouts
    // Select appropriate position in Carla

    @rdfs:label "Navigate-Intersection"
    @dc:description "  ADS determines right-of-way, enters, navigates, and exits 
     intersections, including signalized, stop signs, 4/3/2-ways, and communicates with 
     other road users as necessary; may include left or right turns across oncoming traffic.  "
    concept Navigate-Intersection  < DynamicDrivingTask[
        //restricts maxVelocity to exactly 1
    ]
    // rule; ood must have Intersection
    // refer to 共同研究結果


    @rdfs:label "Navigate-Crosswalk"
    @dc:description "  ADS determines right-of-way, enters, navigates, and exits 
     pedestrian crosswalks, and communicates with other road users as necessary. "
    concept Navigate-Crosswalk  < DynamicDrivingTask[
        //restricts maxVelocity to exactly 1
    ]
        // rule; ood must have Crosswalk  RoadMarking:CrosswalkMarking
        // refer to 共同研究結果

	@rdfs:label "Navigate Work Zone"
	@dc:description "ADS determines right-of-way and traffic patterns, enters, navigates and exits work zone, and communicates with other road users as necessary."
    concept Navigate-Work-Zone < DynamicDrivingTask
    // rule : odd must have work zone:  Zone:WorkZone
    // Select appropriate position in Carla, work zone + static obstacles

	@rdfs:label "N-Point Turn"
	@dc:description "ADS makes a heading adjustment that involves alternating between forward and reverse movement and adjusting steering to reposition the vehicle within a tight space."
    concept N-Point-Turn < DynamicDrivingTask
    // rule: pending
    // pending

	@rdfs:label "U-Turn"
	@dc:description "ADS determines right-of-way, initiates, and completes a U-turn, and communicates with other road users as necessary."
    concept U-Turn < DynamicDrivingTask
    // rule: pending, no U-Turn signs
    // Select appropriate position in Carla

	@rdfs:label "Route Planning"
	@dc:description "ADS uses various information to define (and potentially update) a route network including road segments, turns, etc."
    concept Route-Planning < DynamicDrivingTask
    // pending



    scalar property maxVelocity[
        domain DynamicDrivingTask
        range xsd:float
        functional  
    ]
    scalar property minVelocity[
        domain DynamicDrivingTask
        range xsd:float
        functional  
    ]
    scalar property MinimumTHW[
        domain DynamicDrivingTask
        range xsd:float
        functional
    ]

    scalar property RelativeVelocity[
        domain DynamicDrivingTask
        range xsd:float
        functional
    ]

    scalar property TargetVelocity[
        domain DynamicDrivingTask
        range xsd:float
        functional
    ]
    
    
   


    // scalar property MinimumRelativeDistanceX[
    //     domain DynamicDrivingTask
    //     range xsd:float
    //     functional
    // ]

    // scalar property MinimumRelativeDistanceY[
    //     domain DynamicDrivingTask
    //     range xsd:float
    //     functional
    // ]

    aspect NotValidVel
    aspect NotValidRelativeVelocity
    // aspect NotValidRelativeDistanceX
    // aspect NotValidRelativeDistanceY

    rule low-speed-merge-velocity[
        maxVelocity(ddt, vmax) &
         adsdomain:isPerformedBy(ddt, ads) &
          adsdomain:isEquippedTo(ads, sv) & 
           adsdomain:hasVelocity(sv, vel) &
            adsdomain:hasFloatNumber(vel, value) &
             builtIn ( swrlb:lessThan, vmax, value) -> NotValidVel(vel)
    ]

    // rule low-speed-merge-velocity[
    //     baseline(ddt) &
    //      adsdomain:isPerformedBy(ddt, ads) &
    //       adsdomain:isEquippedTo(ads, sv) & 
    //        adsdomain:hasVelocity(sv, vel) &
    //         adsdomain:hasFloatNumber(vel, value) &
    //          builtIn ( swrlb:lessThan, vmax, value) -> NotValidVel(vel)
    // ]

  



    rule VelocityCheck_VF[
        Vehicle-Following(ddt) &
         adsdomain:isPerformedBy(ddt, ads) &
          adsdomain:isEquippedTo(ads, sv) & 
           adsdomain:isAVOf(sv, ad) &
            adsdomain:hasJASPARODD(ad, odd) &
             adsdomain:hasTP(odd, pov) &
              adsdomain:Vehicle(pov) &
               adsdomain:hasVelocity(sv, vel) &
                adsdomain:hasFloatNumber(vel, value) &
                 adsdomain:hasVelocity(pov, velpov) &
                  adsdomain:hasFloatNumber(velpov, valuepov) &
                   builtIn ( swrlb:lessThan, value, valuepov) -> NotValidRelativeVelocity(vel)

    ]
    
    aspect LeadingVehicle
    
    
    // concept LeadingVehicle < adsdomain:Vehicle // this causes errors
    relation entity Follows
    [
        from FollowingVehicle
        to LeadingVehicle
        forward follows
        reverse isfollowedBy
    ]
    concept FollowingVehicle < adsdomain:AutomatedVehicle[
        restricts follows to min 1
    ]
    

    rule infer-following-vehicle[
        Vehicle-Following(ddt) &
            adsdomain:isPerformedBy(ddt, ads) &
            adsdomain:isEquippedTo(ads, sv) & 
            adsdomain:isAVOf(sv, ad) -> FollowingVehicle(sv)
    ]
    rule infer-leading-following-vehicle[
        Vehicle-Following(ddt) &
            adsdomain:isPerformedBy(ddt, ads) &
            adsdomain:isEquippedTo(ads, sv) & 
            adsdomain:isAVOf(sv, ad) &
            adsdomain:hasJASPARODD(ad, odd) &
                adsdomain:hasTP(odd, pov) &
                adsdomain:Vehicle(pov) -> follows(sv, pov) & LeadingVehicle(pov) 
    ]


    concept SubjectVehicle < adsdomain:AutomatedVehicle
    rule subject-vehicle[
        Low-Speed-Merge(ddt) &
            adsdomain:isPerformedBy(ddt, ads) &
            adsdomain:isEquippedTo(ads, sv) & 
            adsdomain:isAVOf(sv, ad) -> SubjectVehicle(sv)
    ]
    aspect SurroundingVehicle
    relation entity Surrounds
    [
        from Low-Speed-Merge-ODD
        to SurroundingVehicle
        forward surrounds
        reverse isSurroundedBy
    ]
    aspect Low-Speed-Merge-ODD 
    aspect ODD-Baseline < Low-Speed-Merge-ODD[
        restricts surrounds to max 0
    ]
    aspect ODD-Simple-Positive < Low-Speed-Merge-ODD[
        restricts surrounds to exactly 1
    ]
    aspect ODD-Complex-Positive < Low-Speed-Merge-ODD[
        restricts surrounds to exactly 2
    ]
    aspect ODD-Simple-Negative < Low-Speed-Merge-ODD[
        restricts surrounds to min 3
    ]

    
    rule baselinerule[
        Baseline(ddt) &
            adsdomain:isPerformedBy(ddt, ads) &
            adsdomain:isEquippedTo(ads, sv) & 
            adsdomain:isAVOf(sv, ad) &
            adsdomain:hasJASPARODD(ad, odd) &
                adsdomain:hasTP(odd, pov) ->  ODD-Baseline(odd) & SurroundingVehicle(pov) & surrounds(odd, pov)
                // & ODD-Baseline(odd) & surrounds(odd, pov)
    ]

    rule simple-positiverule[
        Simple-Positive(ddt) &
            adsdomain:isPerformedBy(ddt, ads) &
            adsdomain:isEquippedTo(ads, sv) & 
            adsdomain:isAVOf(sv, ad) &
            adsdomain:hasJASPARODD(ad, odd) &
                adsdomain:hasTP(odd, pov) ->  ODD-Simple-Positive(odd) & SurroundingVehicle(pov) & surrounds(odd, pov)
                // & ODD-Baseline(odd) & surrounds(odd, pov)
    ]
    rule complex-positiverule[
        Complex-Positive(ddt) &
            adsdomain:isPerformedBy(ddt, ads) &
            adsdomain:isEquippedTo(ads, sv) & 
            adsdomain:isAVOf(sv, ad) &
            adsdomain:hasJASPARODD(ad, odd) &
                adsdomain:hasTP(odd, pov) ->  ODD-Complex-Positive(odd) & SurroundingVehicle(pov) & surrounds(odd, pov)
                // & ODD-Baseline(odd) & surrounds(odd, pov)
    ]
    rule simple-negativerule[
        Simple-Negative(ddt) &
            adsdomain:isPerformedBy(ddt, ads) &
            adsdomain:isEquippedTo(ads, sv) & 
            adsdomain:isAVOf(sv, ad) &
            adsdomain:hasJASPARODD(ad, odd) &
                adsdomain:hasTP(odd, pov) ->  ODD-Simple-Negative(odd) & SurroundingVehicle(pov) & surrounds(odd, pov)
                // & ODD-Baseline(odd) & surrounds(odd, pov)
    ]

    


    // rule RelativeDistanceX[
    //     MinimumRelativeDistanceX(ddt, minrelX) &
    //      adsdomain:isPerformedBy(ddt, ads) &
    //       adsdomain:isEquippedTo(ads, sv) &
    //        adsdomain:isAVOf(sv, av) &
    //         adsdomain:hasJASPARODD(av, odd) &
    //          adsdomain:hasTP(odd, tp) &
    //           adsdomain:hasPos-X(tp, posX) &
    //            adsdomain:hasFloatNumber(posX, value) &
    //             builtIn ( swrlb:lessThan, value, minrelX) -> NotValidRelativeDistanceX(posX)
    // ]

    // rule RelativeDistanceY[
    //     MinimumRelativeDistanceY(ddt, minrelY) &
    //      adsdomain:isPerformedBy(ddt, ads) &
    //       adsdomain:isEquippedTo(ads, sv) &
    //        adsdomain:isAVOf(sv, av) &
    //         adsdomain:hasJASPARODD(av, odd) &
    //          adsdomain:hasTP(odd, tp) &
    //           adsdomain:hasPos-Y(tp, posY) &
    //            adsdomain:hasFloatNumber(posY, value) &
    //             builtIn ( swrlb:lessThan, value, minrelY) -> NotValidRelativeDistanceY(posY)
    // ]

    


}